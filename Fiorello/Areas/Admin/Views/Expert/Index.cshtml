@model IEnumerable<Expert>

@{
    ViewBag.Title = "Expert";
}

<div style="width: 70%; margin-left: 40px">
    <div style="display: flex; justify-content:right">
        <a asp-area="Admin" asp-controller="Expert" asp-action="Create" style="color: white;background-color: green; border: none" class="btn my-5">Create</a>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th scope="col">Title</th>
                <th scope="col">Operations</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var expert in Model)
            {
                <tr>
                    <td>@expert.Title</td>
                    <td>
                        <div style="display: flex">
                            <a asp-area="Admin" asp-controller="Expert" asp-action="Details" asp-route-id="@expert.Id" style="color: white;background-color: purple" class="btn">Details</a>
                            <a asp-area="Admin" asp-controller="Expert" asp-action="Edit" asp-route-id="@expert.Id" style="color: black; margin-left: 10px; background-color: yellow" class="btn">Edit</a>
                            <form method="post" asp-area="Admin" asp-controller="Expert" asp-action="Delete" asp-route-id="@expert.Id">
                                <button type="submit" style="background-color: red; color: white; margin-left: 10px" class="btn">Delete</button>
                            </form>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>